syntax = "proto3";

package v1;

enum Enum1 {
    ZERO = 0;
    ONE = 1;
    TWO = 2;
    THREE = 3;
    FOUR = 4;
}

message EnumMessage {
    Enum1 enum1_message = 1;
    enum Enum2 {
        STUFF = 0;
        PIE = 1;
    }

    Enum2 enum2_message = 2;
}
